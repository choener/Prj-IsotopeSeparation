# This tool creates plots and graphics for fully trained models.

# TODO Consider this an idea, since we need samples (just like for training) to the posterior predictive.

import argparse

def main ():
    parser = argparse.ArgumentParser()

